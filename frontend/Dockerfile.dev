FROM node:22-alpine

WORKDIR /app
COPY package*.json ./
RUN npm install

# Vite 监听 80 端口（与生产 Nginx 一致），
# 这样 docker-compose.yml 中的 5173:80 映射在开发/生产环境都通用
EXPOSE 80
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "80"]
