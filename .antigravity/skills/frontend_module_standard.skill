# Playground 无限画布模块开发规范

## 适用范围
本规范**仅适用于** `frontend/src/pages/Playground/` 无限画布创作中心模块。

---

## 目录结构

```
Playground/
├── Playground.tsx              # 入口编排壳（仅挂载 Provider + 组合子组件，控制在 80 行以内）
├── Playground.css              # 页面级全局样式覆盖
├── types.ts                    # 所有 TypeScript 类型/接口定义
├── constants.ts                # 常量（分辨率映射表）、纯函数工具（类别图标/标签）
├── context/
│   └── PlaygroundContext.tsx    # 双 Context 状态管理（高频画布 + 低频业务）
├── hooks/
│   ├── useCanvasInteraction.ts  # 画布平移/缩放/节点拖拽/面板拖拽的鼠标事件逻辑
│   └── useGeneration.ts         # API 生成调用 + 视频任务轮询 + 节点生命周期
├── components/
│   ├── InfiniteCanvas.tsx       # 无限画布容器（网格背景 + 变换层 + 鼠标事件代理）
│   ├── FloatingHeader.tsx       # 左上角悬浮"创作中心"标头
│   ├── FloatingToolbar.tsx      # 左侧工具胶囊栏（指针/抓手/画笔/重置视图）
│   ├── PromptInput.tsx          # 底部悬浮提示词输入框 + API密钥选择 + 生成按钮
│   ├── SettingsWidget.tsx       # 右侧可拖拽可折叠的悬浮参数面板（含类别切换+模型选择+参数面板）
│   ├── ParamControl.tsx         # 单个参数控件渲染器（radio/select/number/switch/input）
│   ├── ModelDrawer.tsx          # 模型全景选择器抽屉
│   ├── TokenModal.tsx           # API 密钥选择弹窗
│   └── nodes/                   # 可扩展节点系统
│       ├── CanvasNode.tsx       # 节点外壳（标题栏 + 拖拽手柄 + 关闭按钮 + 内容委托）
│       ├── VideoNodeContent.tsx # 视频类型节点内容
│       ├── ImageNodeContent.tsx # 图片类型节点内容
│       └── TextNodeContent.tsx  # 文本类型节点内容
```

---

## 关键架构规则

### 1. 双 Context 性能分离
`PlaygroundContext.tsx` 内部维护**两个独立 Context**：
- **CanvasContext**（高频）：画布变换 `canvasTransform`、拖拽状态、节点数组 `nodes` 等 —— 仅 `InfiniteCanvas` 和 `CanvasNode` 订阅
- **PlaygroundContext**（低频）：模型列表、参数值、token、UI 开关等 —— `SettingsWidget`、`PromptInput` 等面板组件订阅

> 这样画布拖拽时不会触发参数面板等无关组件重新渲染。

### 2. 节点系统扩展方式
新增节点类型（如音频、3D模型等）：
1. 在 `types.ts` 的 `CanvasNode.type` 联合类型中添加新值
2. 在 `nodes/` 下新建 `XxxNodeContent.tsx`
3. 在 `CanvasNode.tsx` 的内容区域添加一个条件渲染分支

### 3. 组件约束
- 所有组件使用 `React.memo()` 包裹并设置 `displayName`
- 组件通过 `useCanvas()` / `usePlayground()` 消费 Context 状态
- 事件处理函数使用 `useCallback()`，派生数据使用 `useMemo()`
- 禁止 props drilling 超过 2 层

### 4. 状态读写路径
- 组件 → `useCanvas()` / `usePlayground()` → Context
- 交互事件 → `useCanvasInteraction` hook
- API 调用 → `useGeneration` hook
