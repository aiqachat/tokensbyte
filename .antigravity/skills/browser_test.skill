# 浏览器测试技能 (Browser Test Skill)

当需要在浏览器中测试本地开发环境时，请遵循以下规则：

---

## 1. 测试地址

- **管理后台**：`http://localhost:5173/admin0755`
- **用户端**：`http://localhost:5173`
- **后端 API**：`http://localhost:3000`

---

## 2. 测试账号

### 管理后台（Admin）
- **用户名**：`admin`
- **密码**：`123456`
- **权限**：管理员，可访问所有管理后台功能（站点插件、模型管理、用户管理等）

### 用户端（User）
- **用户名**：`bubyday`
- **密码**：`diaoni123`
- **权限**：普通用户，用于测试用户侧功能（体验中心、令牌管理、资产管理等）

---

## 3. 登录流程

### 管理后台登录
1. 打开 `http://localhost:5173/admin0755`（管理后台专用入口）
2. 输入用户名 `admin`，密码 `123456`
3. 点击「登录系统」按钮
4. 登录后进入管理后台，左侧有管理菜单（仪表盘、站点插件、模型管理、用户管理等）

### 用户端登录
1. 打开 `http://localhost:5173`（用户端入口）
2. 输入用户名 `bubyday`，密码 `diaoni123`
3. 点击「登录系统」按钮
4. 登录后进入用户界面，可访问模型体验、令牌管理、资产管理等功能

> **注意**：管理后台和用户端是**两个不同的登录地址**，不要混用。管理员走 `/admin0755`，用户走根路径 `/`。

---

## 4. 常用测试路径

### 管理后台
- 站点插件 → 模型体验中心 → 体验模型管理 / 体验方案配置
- 模型管理 → 模型列表
- 用户管理

### 用户端
- 模型体验（Playground）：用户侧体验中心
- 令牌管理：API Token 管理
- 资产管理：文件存储

---

## 5. 注意事项

- 管理员和普通用户使用同一个登录入口，系统根据角色自动切换界面
- 测试用户端功能前，需要先在管理后台开启对应的插件和模型体验开关
- 如果登录后跳转到空白页，尝试手动访问 `http://localhost:5173/` 根路径
