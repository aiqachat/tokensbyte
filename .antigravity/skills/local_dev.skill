# TokensByte 调试与运行技能 (Local Dev Skill)

当用户发出以下指令时，请遵循相应的步骤进行操作：

## 1. Docker 调试测试
**触发词**：`docker 的调试测试`, `docker 调试测试`, `docker 调试`

**执行前置检查与清理**：
1. **清理本地进程**：检查并关闭可能占用端口 3000 (Backend) 和 5173 (Frontend) 的本地进程。
   ```bash
   # 查找并杀掉占用端口的进程
   lsof -i :3000 -t | xargs kill -9
   lsof -i :5173 -t | xargs kill -9
   ```
2. **停止 Docker 容器**：确保旧容器已完全移除。
   ```bash
   docker-compose down
   ```

**执行步骤**：
1. **构建并启动新容器**：
   ```bash
   docker-compose up -d --build
   ```
2. **验证状态**：
   ```bash
   docker-compose ps
   ```

---

## 2. 本地调试测试 (快速迭代)
**触发词**：`本地测试`, `本地调试`, `本地运行`

**执行前置检查与清理**：
1. **停止 Docker 容器**：防止 Docker 占用端口。
   ```bash
   docker-compose down
   ```
2. **清理本地残留进程**：
   ```bash
   lsof -i :3000 -t | xargs kill -9
   lsof -i :5173 -t | xargs kill -9
   ```

**执行步骤**：
> **注意**：本地调试直接在宿主机运行，跳过 Docker 构建，速度更快。

1. **启动后端服务**：
   - 进入 `backend` 目录，执行：
     ```bash
     cd backend && cargo run
     ```
2. **启动前端服务**：
   - 进入 `frontend` 目录，执行：
     ```bash
     cd frontend && npm run dev
     ```

3. **并发说明**：
   - 如果可能，可以使用 `concurrently` 或在两个不同的终端中分别启动上述服务。

---

## 3. 环境变量参考
- **后端**：`DATABASE_URL` 默认已配置为支持本地 PostgreSQL。
- **前端**：`VITE_API_BASE` 默认通过代理转发。

---

**技能保存位置**：`.antigravity/skills/local_dev.skill`
