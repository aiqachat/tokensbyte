# TokensByte 调试与运行技能 (Local Dev Skill)

当用户发出以下指令时，请遵循相应的步骤进行操作：

## 0. Docker 路径自动检测（全局前置）
> 在执行任何 Docker 相关命令前，先确定 Docker CLI 的可用路径。
> macOS 上 Docker Desktop 的 CLI 通常在 `/usr/local/bin/docker`，而 `docker` 可能不在 shell PATH 中。

```bash
# 检测 Docker CLI 可用路径
DOCKER_BIN=$(command -v docker 2>/dev/null || echo "/usr/local/bin/docker")
$DOCKER_BIN --version
```

- 如果 `docker` 不在 PATH 中，后续所有命令一律使用 `$DOCKER_BIN` 或 `/usr/local/bin/docker` 代替。
- 使用 `$DOCKER_BIN compose` 代替 `docker-compose`（Compose V2 已内置于 Docker CLI）。

---

## 1. Docker 全栈调试测试（容器化构建）
**触发词**：`docker 的调试测试`, `docker 调试测试`, `docker 调试`, `docker 打包`

**执行前置检查与清理**：
1. **清理本地进程**：检查并关闭可能占用端口 3000 (Backend) 和 5173 (Frontend) 的本地进程。
   ```bash
   lsof -i :3000 -t | xargs kill -9 2>/dev/null
   lsof -i :5173 -t | xargs kill -9 2>/dev/null
   ```
2. **停止并移除旧容器**：确保旧容器已完全移除。
   ```bash
   $DOCKER_BIN compose down --remove-orphans
   ```

**执行步骤**：
1. **构建并启动全部容器**（包含 postgres + backend + frontend）：
   ```bash
   $DOCKER_BIN compose up -d --build
   ```
2. **验证状态**：
   ```bash
   $DOCKER_BIN compose ps
   ```
3. **查看后端日志**（可选，排查启动问题）：
   ```bash
   $DOCKER_BIN compose logs -f backend --tail=50
   ```

---

## 2. 本地调试测试（快速迭代，推荐日常开发）
**触发词**：`本地测试`, `本地调试`, `本地运行`, `后端服务`, `重启服务`, `重新编译`, `本地测试环境重启编译`

> **核心策略**：仅用 Docker 运行 PostgreSQL 数据库，后端和前端在宿主机直接运行，实现最快的编译-测试循环。
> **禁止使用 SQLite**：本项目始终使用 PostgreSQL，即使本地没有数据也不需要回退到 SQLite。

### 2.1 前置检查与清理

1. **停止全栈 Docker 容器**（防止端口冲突，但保留 postgres 容器）：
   ```bash
   $DOCKER_BIN compose stop backend frontend 2>/dev/null
   $DOCKER_BIN compose rm -f backend frontend 2>/dev/null
   ```
2. **清理本地残留进程**：
   ```bash
   lsof -i :3000 -t | xargs kill -9 2>/dev/null
   lsof -i :5173 -t | xargs kill -9 2>/dev/null
   ```

### 2.2 启动 Docker PostgreSQL 数据库

1. **检查 PostgreSQL 容器是否已运行**：
   ```bash
   $DOCKER_BIN ps --filter "name=tokensbyte-postgres" --format '{{.Status}}'
   ```
2. **如果未运行则启动**（只启动数据库服务，不构建应用容器）：
   ```bash
   # 移除可能存在的冲突旧容器
   $DOCKER_BIN rm -f tokensbyte-postgres 2>/dev/null
   # 只启动 postgres 服务
   $DOCKER_BIN compose up -d postgres
   ```
3. **等待数据库就绪并验证连接**：
   ```bash
   # 最多等待 15 秒
   for i in $(seq 1 15); do
     $DOCKER_BIN exec tokensbyte-postgres pg_isready -U tokensapi -d tokensapi && break
     echo "⏳ 等待 PostgreSQL 就绪... ($i/15)"
     sleep 1
   done
   ```

### 2.3 确认 backend/.env 配置

> `backend/.env` 中的 `DATABASE_URL` 必须指向 **localhost**（宿主机通过端口映射连接容器内的 PostgreSQL）。
> 根目录 `.env` 中 `DATABASE_URL` 使用 `postgres` 容器名（仅供 Docker Compose 内部网络使用）。

**backend/.env 应包含**：
```
DATABASE_URL=postgres://tokensapi:tokensapi@localhost:5432/tokensapi
```

如果当前 `backend/.env` 中使用了 `@postgres:5432`（容器内网络地址），需要改为 `@localhost:5432`。

### 2.4 启动后端服务

- 进入 `backend` 目录，执行：
  ```bash
  cd backend && cargo run
  ```
- 确认后端日志输出 `✅ Database connection established.` 和 `✅ TokensByte server is now completely online`。

### 2.5 启动前端服务

- 进入 `frontend` 目录，执行：
  ```bash
  cd frontend && npm run dev
  ```

### 2.6 并发说明
- 后端和前端需要在**两个不同的终端**中分别启动（后端是阻塞式运行）。
- 管理后台和用户后台如果有改动都需要重启对应服务。

---

## 3. 仅启动数据库（用于数据库调试或迁移）
**触发词**：`启动数据库`, `数据库调试`, `链接数据库`

**执行步骤**：
1. 按照 2.2 节启动 Docker PostgreSQL。
2. 验证连接：
   ```bash
   $DOCKER_BIN exec tokensbyte-postgres psql -U tokensapi -d tokensapi -c "SELECT version();"
   ```
3. 交互式 psql（可选）：
   ```bash
   $DOCKER_BIN exec -it tokensbyte-postgres psql -U tokensapi -d tokensapi
   ```

---

## 4. 环境变量参考

| 变量 | 用途 | 本地调试值 | Docker 内部值 |
|---|---|---|---|
| `DATABASE_URL` | PostgreSQL 连接 | `postgres://tokensapi:tokensapi@localhost:5432/tokensapi` | `postgres://tokensapi:tokensapi@postgres:5432/tokensapi` |
| `JWT_SECRET` | JWT 签名密钥 | `tokensbyte-change-me` | 同左 |
| `ADMIN_USERNAME` | 管理员用户名 | `admin` | 同左 |
| `ADMIN_PASSWORD` | 管理员密码 | `123456` | 同左 |
| `REGISTER_ENABLED` | 是否开放注册 | `true` | 同左 |

### 关键区别
- **`backend/.env`**：本地调试使用，`DATABASE_URL` 指向 `localhost:5432`
- **根目录 `.env`**：Docker Compose 使用，`DATABASE_URL` 指向 `postgres:5432`（容器服务名）

---

## 5. 常见问题排查

### 数据库连接失败
1. 检查 Docker Desktop 是否已启动
2. 检查 PostgreSQL 容器状态：`$DOCKER_BIN ps --filter "name=tokensbyte-postgres"`
3. 检查端口 5432 是否被占用：`lsof -i :5432`
4. 查看容器日志：`$DOCKER_BIN logs tokensbyte-postgres --tail=20`

### 端口冲突
- 确保在切换「Docker 全栈模式」和「本地调试模式」时，先清理另一种模式的进程/容器

---

**技能保存位置**：`.antigravity/skills/local_dev.skill`
